#!/bin/sh
set -eu

container="${1:-${GLUETUN_CONTAINER:-gluetun}}"

if ! command -v docker >/dev/null 2>&1; then
  echo "docker CLI is not installed in this image" >&2
  exit 127
fi

docker restart "$container"
